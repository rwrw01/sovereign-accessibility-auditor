# Database
DATABASE_URL=postgresql://saa:saa_dev_password@localhost:5432/saa

# API
HOST=127.0.0.1
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# JWT (VERPLICHT — genereer met: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=
JWT_ACCESS_EXPIRY=900

# Redis / Valkey
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# OIDC / Keycloak (optioneel)
# OIDC_ISSUER=https://keycloak.example.com/realms/saa
# OIDC_CLIENT_ID=saa-dashboard
# OIDC_CLIENT_SECRET=
# OIDC_REDIRECT_URI=http://localhost:3001/api/v1/auth/oidc/callback

# Dashboard
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_OIDC_ENABLED=false
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
